<h1>Books</h1>
    <% @books.each do |book| %>
    <div>
        <%= book.title %>
        <%= book.author %>
        <%= book.year %>

        <% @inventory.each do |i| %>
          <% @stores.each do |s| %>
              <%if i.store_id == s.id%>
              <%= s.codename%>
              <%end%>
          <%end%>
          <%= i .quantity%>
         <%end%>   
        <%= link_to 'Edit', edit_book_path(book) %>
    </div>
    <% end %>
<%= link_to 'New Book', new_book_path %>
